// 读取输入是用readline()函数,读入的是一个字符串,然后自己处理一下拿到数据
// 编译器信息
// 版本：d8 6.0
// 输入输出处理
// 1. 核心代码模式处理
// 不需要处理任何输入输出，直接返回值即可。
// 2. ACM 模式
// 你的代码需要处理输入输出，请使用如下样例代码读取输入和打印输出：

function input(){
    while (line = readline()) {
        var lines = line.split(' ');
        var a = parseInt(lines[0]);
        var b = parseInt(lines[1]);
        print(a + b);
        console.log(str);
    }
}
